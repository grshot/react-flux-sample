"use strict";

var React = require('react');

var About = React.createClass({
    render: function(){
        return (
            <div>About this sample... a demo testing for React + Flux</div>
        );
    }
});

module.exports = About;